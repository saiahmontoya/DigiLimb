<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DigiLimbDesktop"
             x:Class="DigiLimbDesktop.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- 🔹 Global Button Style with Hover Effect & Rounded Shadow -->
            <Color x:Key="PageBackgroundColor">White</Color>
            <Color x:Key="CardBackgroundColor">LightBlue</Color>
            <Color x:Key="PrimaryTextColor">#2D2D2D</Color>
            <Color x:Key="SecondaryTextColor">DarkGray</Color>
            <Color x:Key="EntryBackgroundColor">White</Color>
            <Color x:Key="ShellColor">#C0C0C0</Color>
            <!-- Light theme default -->

            <Style x:Key="HoverButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#037ffc"/>
                <!-- Default color -->
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="MinimumHeightRequest" Value="55"/>
                <Setter Property="MinimumWidthRequest" Value="250"/>

                <!-- ✅ Define Default Rounded Shadow -->
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Opacity="0.2" Offset="3,3" Radius="10"/>
                </Setter>

                <!-- ✅ Define Hover Effect & Stronger Rounded Shadow on Hover -->
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#037ffc"/>
                                    <!-- Ensures default color -->
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.2" Offset="3,3" Radius="10"/>
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#026DD9"/>
                                    <!-- Darker blue on hover -->
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.4" Offset="5,5" Radius="12"/>
                                        <!-- Softer, rounder shadow -->
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>


            <Style x:Key="SelectableButtonStyle" TargetType="Button">
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="MinimumHeightRequest" Value="55"/>
                <Setter Property="MinimumWidthRequest" Value="120"/>
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="BackgroundColor" Value="#D3D3D3"/>
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Opacity="0.2" Offset="3,3" Radius="10"/>
                </Setter>

                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">

                            <!-- ✅ Normal (default, unselected, unhovered) -->
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#D3D3D3"/>
                                    <Setter Property="TextColor" Value="Black"/>
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.2" Offset="3,3" Radius="10"/>
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>

                            <!-- ✅ Selected (hovering does NOT override this) -->
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#037ffc"/>
                                    <Setter Property="TextColor" Value="White"/>
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.5" Offset="4,4" Radius="12"/>
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>

                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>



            <!-- 🔹 Quit Button Style (With Hover Effect & Rounded Shadow) -->
            <Style x:Key="QuitButtonStyle" TargetType="Button" BasedOn="{StaticResource HoverButtonStyle}">
                <Setter Property="BackgroundColor" Value="#4e92bf"/>
                <!-- Default red -->
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#4e92bf"/>
                                    <!-- Ensures it returns to red -->
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.2" Offset="3,3" Radius="10"/>
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#3B779F"/>
                                    <!-- Darker red on hover -->
                                    <Setter Property="Shadow">
                                        <Shadow Brush="Black" Opacity="0.4" Offset="5,5" Radius="12"/>
                                        <!-- Softer, rounder shadow -->
                                    </Setter>
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

           

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
